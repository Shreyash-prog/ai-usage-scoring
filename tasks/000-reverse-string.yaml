id: "000"
title: "Reverse a String"
type: "code"
description_md: |
  Write a function `reverse(s: str) -> str` that returns the input string
  reversed.

  Example: `reverse("hello")` -> `"olleh"`.

  Trivial placeholder used only for UI/plumbing testing during development. Not in
  the default session sequence; pass an explicit task_sequence to use it.
starter_code: |
  def reverse(s: str) -> str:
      # your code here
      pass
test_code: |
  assert reverse("hello") == "olleh"
  assert reverse("") == ""
  assert reverse("a") == "a"
  print("OK")
time_limit_minutes: 10
baseline_prompts: 3
expected_signals:
  recent_error_likely: false
  ai_codeblock_likely: true
